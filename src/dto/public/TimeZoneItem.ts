export type TimeZoneItem = {
  /**
   * IANA Id
   * IANA 编号
   */
  id: string;

  /**
   * Display name
   * 显示名称
   */
  displayName: string;

  /**
   * Standard name
   * 标准名称
   */
  standardName: string;

  /**
   * UTC offset
   * UTC偏移
   */
  utcOffset: string;
};
